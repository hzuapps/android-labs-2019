<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.hzuapps.androidlabs.soft1714080902329">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Brother"
            android:label="@string/title_activity_brother"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">

        </activity>
        <activity
            android:name=".Soft1714080902329Activity"
            android:exported="true"
            android:label="@string/title_activity_soft1714080902329"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>